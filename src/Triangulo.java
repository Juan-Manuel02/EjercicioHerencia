abstract class Triangulo extends Poligono {

        // Constructor que llama al constructor de Poligono
        public Triangulo(double base, double altura) {
            super(base, altura);
        }
    }

