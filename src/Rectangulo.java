import java.awt.*;
abstract class Rectangulo extends Poligono {

    // Constructor que llama al constructor de Poligono
    public Rectangulo(double base, double altura) {
        super(base, altura);
    }
}
